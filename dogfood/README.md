# anoikis-tools dogfood

The dogfood attestation for switchover condition (iii): a real project driven design -> plan -> execution end-to-end through this checkout's own compiled `anoikis` binary and its `policy/agents/anoikis-*` roster, via genuine independent Claude Code sessions.

Read [`attestation.md`](attestation.md) first — it is the record itself. `evidence/` holds everything it cites: the design, the final graph state, the append-only run log, both gate-review verdicts, every real session's id/transcript/cost, and a full source snapshot of what actually merged to `main`.
